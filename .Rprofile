# Activate the renv project library if it has been set up. Guarded so a fresh
# clone (before `Rscript setup.R`) still starts R without error.
if (file.exists("renv/activate.R")) source("renv/activate.R")
